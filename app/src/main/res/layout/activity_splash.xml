<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_splash_luxury">

    <!-- top cloth glow -->
    <View
        android:layout_width="240dp"
        android:layout_height="240dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-90dp"
        android:background="@drawable/bg_splash_glow"/>

    <!-- bottom glow -->
    <View
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="-80dp"
        android:layout_marginEnd="-60dp"
        android:background="@drawable/bg_splash_glow_bottom"/>

    <!-- center content -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <!-- logo card -->
        <FrameLayout
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:background="@drawable/bg_logo_circle"
            android:padding="5sp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/icon2"
                android:scaleType="fitCenter"/>
        </FrameLayout>

        <!-- app name -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="SVADHA"
            android:textColor="#FFF4E3"
            android:textSize="38sp"
            android:textStyle="bold"
            android:letterSpacing="0.14"/>

        <!-- premium line -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Luxury Chaniya Choli Rental"
            android:textColor="#D7B98C"
            android:textSize="14sp"
            android:letterSpacing="0.05"/>

        <!-- decorative divider -->
        <View
            android:layout_width="90dp"
            android:layout_height="2dp"
            android:layout_marginTop="22dp"
            android:background="#C9A56A"/>

    </LinearLayout>

    <!-- footer -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:text="Crafted with Elegance"
        android:textColor="#8F7A63"
        android:textSize="12sp"
        android:letterSpacing="0.08"/>

</RelativeLayout>